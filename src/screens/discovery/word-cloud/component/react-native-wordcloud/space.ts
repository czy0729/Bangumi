// @ts-nocheck
export default class Space {
  constructor(spaceType, width, height, x, y) {
    this.spaceType = spaceType
    this.width = width
    this.height = height
    this.x = x
    this.y = y
  }
}
